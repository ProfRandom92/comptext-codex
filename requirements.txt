# Core dependencies
# CompText-Codex now relies exclusively on the Python standard library.
# This keeps the package functional even in offline or proxied environments.

# Optional: Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# Optional: Development
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
isort>=5.12.0

# Optional: Documentation
mkdocs>=1.5.0
mkdocs-material>=9.1.0
